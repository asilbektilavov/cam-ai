# ============================================
# CamAI — Переменные окружения
# ============================================
# Скопируйте в .env и заполните значения:
#   cp .env.example .env
#
# ВАЖНО: Никогда не коммитьте .env в git!
# ============================================

# --- База данных ---
# SQLite (путь к файлу)
# Docker:    file:/app/data/cam-ai.db
# Локально:  file:./dev.db
DATABASE_URL="file:./dev.db"

# --- Аутентификация ---
# ОБЯЗАТЕЛЬНО: Секретный ключ для JWT (мин. 32 символа)
# Сгенерировать: openssl rand -base64 32
NEXTAUTH_SECRET=""

# Публичный URL приложения
# Production: https://your-domain.com или https://192.168.1.100
# Локально:   http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"

# --- ИИ-анализ ---
# Google Gemini API ключ (бесплатно: 15 запросов/мин, 1500/день)
# Получить: https://aistudio.google.com/apikey
# Оставьте пустым — ИИ-анализ будет отключён
GEMINI_API_KEY=""

# --- Telegram бот ---
# Токен бота для уведомлений (техник создаёт через @BotFather)
# При установке: bash install-client.sh --telegram 7123456:AAF...
TELEGRAM_BOT_TOKEN=""

# --- Мульти-филиал (синхронизация) ---
# Роль инстанса: "central" | "satellite" | "" (standalone)
INSTANCE_ROLE=""

# Уникальный ID инстанса (генерируется автоматически если пусто)
INSTANCE_ID=""

# [Satellite] URL центрального сервера для push данных
# Пример: https://romashka.camvision.com
SYNC_TO=""

# [Central + Satellite] Общий ключ синхронизации (одинаковый на всех)
SYNC_KEY=""

# [Satellite] Интервал синхронизации в секундах (по умолчанию 300 = 5 мин)
SYNC_INTERVAL="300"

# --- Cloudflare Tunnel ---
# Токен для Cloudflare Tunnel (для доступа через интернет без статического IP)
CLOUDFLARE_TUNNEL_TOKEN=""

# --- Production (опционально) ---
# Доменное имя для SSL сертификата (Let's Encrypt)
# DOMAIN=cam-ai.example.com

# Email для уведомлений Let's Encrypt
# CERTBOT_EMAIL=admin@example.com
